esphome:
  name: sunrise-test

esp32:
  board: esp32dev

external_components:
  - source:
      type: local
      path: components

logger:

i2c:
  sda: GPIO21
  scl: GPIO22

sensor:
  - platform: senseair_sunrise
    co2:
      name: "CO2"
    update_interval: 60s
